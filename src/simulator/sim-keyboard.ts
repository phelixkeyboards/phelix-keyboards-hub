/*
electron app 
.exe --- OS

User opens app
user sets some settings for the keyboard
    -keybindings
    -install some widgets
QMK firmware fork

*/

class KBSimulator {
    id: string;

    constructor(id: string) {
        this.id = id;
    }

    

}